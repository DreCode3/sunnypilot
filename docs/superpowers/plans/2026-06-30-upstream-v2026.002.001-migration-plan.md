# Upstream Migration Plan — sunnypilot v2026.002.001 (2026-06-30)

Goal: bring the `2021_explorer_st-mici` fork onto the new sunnypilot release **v2026.002.001**
(`upstream/master` `31dc4d8e52`, opendbc `sunnypilot/master` `8b7e933ba`, both 2026-06-28) while
preserving all Explorer ST lateral + longitudinal customizations. **Analysis/planning only — no
rebase, push, or device action without explicit approval.** Measured, not assumed: the conflict
surface is ~9 small files; `carcontroller.py` (+772 lines) reduces to a one-line import fix.

## Decision (UPDATED 2026-06-30): FULL-currency MERGE of upstream into our branch
Driver wants to bring in **all** the new sunnylink/UI/release housekeeping (be feature/fix-current),
not a Ford-only cherry-pick. Mechanically this is the SAME `git merge upstream/master` already
simulated — bringing "everything" does **not** add textual conflicts (still 7 main + 2 opendbc); it
expands the **semantic** surface (the 369 deleted files, the cereal change, the modeld restructure
now actually land in our tree). So the work splits into: resolve the 9 textual conflicts + clear the
**semantic watchlist** below + run the **validation gauntlet** before trusting it.
- Not wipe-and-reapply (would re-derive ~1,200 lines of tuned control by hand; more work/risk, lost history).
- Not a naive `git rebase` (replays 186/102 commits, **0 patch-id dedup** → repeated conflicts).
- **Do NOT wipe the device** `/data/params`: resets learned `LiveParameters`, calibration, `LaneBiasIntegral` warm-start, model bundles → re-learning regression. Code and device-param state are independent.

## ⚠️ SEMANTIC CONFLICT WATCHLIST — the merge-clean-but-breaks-at-runtime traps
> The last full merge caused multi-day bug-fighting. Textual conflicts (the 9 files) are NOT what
> bit; these are. Each merges with no `<<<<<<<` marker yet breaks or silently drops a customization.
> Hunted down 2026-06-30; each has a located resolution.

**Driving code (opendbc) — VERIFIED SAFE (checks passed, no action beyond the import conflict):**
- ✅ `apply_std_steer_angle_limits` new 6-arg signature matches our call sites (carcontroller.py:55, :452).
- ✅ #3414 `AngleSteeringLimits` split → our `values.py` builds the slim 3-field shape; compatible.
- ✅ #3338 removed `car.capnp brake @5` — we read `CS.out.brakePressed` (survives), not `brake`. No impact.
- ✅ `fordcan.py`, `interface.py`, `ford.h` unchanged upstream → our edits land untouched.

**Main repo — REAL RISK ZONE (verify each; do NOT assume the clean merge is correct):**
1. **`selfdrive/monitoring/helpers.py` DELETED upstream** — our `_PHONE_THRESH=0.8` lived there. ✅ Resolution: ACCEPT the deletion, re-apply 0.8 at the new home **`selfdrive/monitoring/policy.py:46`** (upstream default 0.5). A "keep helpers.py" resolution resurrects a dead file → customization silently lost.
2. **modeld directory restructure** (`common/model.h` deleted; `selfdrive/modeld/` + `sunnypilot/modeld_v2/` rewritten; model artifacts split policy→on/off-policy; `compile_modeld.py`/`compile_dm_warp.py` added; `warp.py`/`buffer`/`is_20hz` logic refactored to `bundle.is20hz`/`_combined_model_type`). Impacts:
   - **LAT_SMOOTH=0.1**: the mechanism SURVIVES (`sunnypilot/modeld_v2/modeld.py:91` `overrides.get('lat','.0')`, smooth at :252, `lat_delay` at :371). Confirm HOW our 0.1 is injected (override/param/bundle) and that it's still 0.1 post-merge (upstream default is 0.0 / ".0").
   - **On-device behavior**: the buffer/`is_20hz` refactor can change actual frame-pairing → could shift the weave itself. Re-validate model inference loads the bundle + produces sane `desiredCurvature`.
   - **`model_replay_sim`**: built against the OLD modeld layout (hardcodes `img_buffer_length=5` per old `modeld.py:75`). It will NOT reflect the new on-device logic until re-anchored. Re-run the CD210/Nevada anchors after merge before trusting any new weave number.
3. **`cereal/log.capnp` DM schema change** — `DriverMonitoringState`→`…DEPRECATED` + new struct; DM model-output fields removed (`eyesVisibleProb`, eye/blink probs, sunglassesProb). Cross-process serialization. Our lateral/long code doesn't read these, but the DM pipeline + our PHONE_THRESH ride on it → verify DM still runs and our threshold still has effect in the rewritten `policy.py`.
4. **`common/params_keys.h`** — did NOT textually conflict, but CONFIRM our 4 custom keys (FordPath4Enabled, LaneBiasIntegral, disable_BP_long_UI, disable_downhill_comp_UI) survive post-merge (`grep`). Hand-add any new upstream key; never wholesale-copy.
5. **369 upstream deletions** (acados/third_party/tici/nav/etc.) — mostly irrelevant, but grep our customized files' imports against the deleted set before building (catch a stray import of a removed module).

## Validation gauntlet — the anti-bug-fight process (run ALL before trusting the merge)
This is the step that was skipped last time. Order: cheapest/earliest-failing first.
1. **Customization-survival audit** — a grep script asserting every knob is PRESENT *and wired* post-merge: `LAT_SMOOTH`(=0.1), `_PHONE_THRESH`(=0.8 in policy.py), smooth_tau (0.12,0.04), lc_kp/PI config, pc_blend schedule, the 4 params_keys, FordPath4, ford.h limits. Fail = lost customization.
2. **Import/compile gate** — `python -c "import ..."` every customized module + a full `scons -j8` in both repos. Catches file-move/rename/deleted-import breaks (the #1 silent killer).
3. **Cross-process schema gate** — run upstream's own `cereal/messaging/tests/validate_sp_cereal_upstream.py` (it exists in the incoming set) + the cereal/services tests. Catches capnp/log schema mismatches.
4. **Unit/safety tests** — opendbc safety + Ford + MADS suites; main repo modeld_v2 tests (`test_warp.py`, the new `test_compile_modeld.py`), monitoring tests.
5. **Process-replay parity (gold standard)** — replay a saved route through the carcontroller pipeline pre- vs post-merge (`explorer_st_logs/pipeline_simulator.py`) and the model path; the final curvature command must match pre-merge to <1e-5 1/m (our control logic is unchanged, so any divergence = a semantic break introduced by the merge). This single test would have caught the last bug-fight.
6. **modeld re-anchor** — re-run `model_replay_sim` CD210/Nevada anchors; confirm corr still ≥0.95 (or update the sim to the new buffer logic) so our analysis tooling stays valid.
7. **Only then** device: controlled checkout (NOT wipe), grep live config, short shakedown drive.

## Behind-counts (A)
| Repo | base | behind | ahead (ours) |
|---|---|---|---|
| main `upstream/master` 31dc4d8e52 | merge-base 2026-04-19 | 248 | 186 (mostly analysis tooling) |
| opendbc `sunnypilot/master` 8b7e933ba | merge-base 2026-03-04 | 150 | 102 |

## Conflict set (B)
**opendbc (2 files, 1 hunk each):**
- `opendbc/car/ford/carcontroller.py` — import-line union (keep `carlog` import + lateral import; `AVERAGE_ROAD_ROLL` is locally redefined).
- `opendbc/safety/tests/libsafety/libsafety_py.py` — test harness; take upstream, re-apply MADS test hooks.

**main (7 files):** `selfdrive/modeld/modeld.py` (LAT_SMOOTH=0.1), `selfdrive/monitoring/helpers.py` (`_PHONE_THRESH=0.8`), `common/model.h`, `sunnypilot/modeld_v2/SConscript`, `sunnypilot/models/tests/model_hash` (the new modeld directory restructure), `sunnypilot/sunnylink/params_metadata.json` (take theirs), `opendbc_repo` (submodule pointer).

**Semantic checks:** ✅ fordcan/interface/ford.h unchanged upstream; ✅ #3414 `AngleSteeringLimits` already-compatible (our 3-field construction matches the new slim shape); ⚠️ verify `lateral.h` #456 (`controls_allowed_lateral`, MADS) and the modeld restructure (LAT_SMOOTH location + warp 4→2 indexing).

---

## C — Step-by-step migration

Work on a throwaway integration branch; never on `2021_explorer_st-mici` directly until validated.

### Phase 0 — prep (no changes)
1. `git fetch upstream && (cd opendbc_repo && git fetch sunnypilot)` (already done 2026-06-30).
2. Commit or stash the working-tree analysis edits first (the tree currently has uncommitted `retrospective_lateral/` + docs changes — keep them out of the merge).
3. Branch: `git switch -c migrate-v2026.002.001 2021_explorer_st-mici` (and same name in `opendbc_repo`).

### Phase 1 — opendbc first (the submodule must be ready before the superproject)
4. In `opendbc_repo` on the integration branch: `git merge sunnypilot/master`.
5. Resolve `carcontroller.py` (1 hunk): keep BOTH `from opendbc.car.carlog import carlog` and `from opendbc.car.lateral import ISO_LATERAL_ACCEL, apply_std_steer_angle_limits`. (Drop `AVERAGE_ROAD_ROLL` from the import — it's redefined locally at the top of our carcontroller.)
6. Resolve `libsafety_py.py`: take upstream structure, re-apply our MADS test hooks (compare against our side of the conflict).
7. **Landmine checks (verify, don't assume):**
   - `params_keys.h` equivalent / `AngleSteeringLimits`: confirmed compatible — our `values.py` builds the slim 3-field form. Re-run `python -c "import opendbc.car.ford.values"` to be sure it imports.
   - `lateral.h` #456: diff our MADS lateral-safety edits against the new `controls_allowed_lateral`; ensure our Ford lat gating still holds.
   - `CarState.brake`→`brakeDEPRECATED` coupling (if it lands): car.capnp rename + ford `carstate.py` must move together.
8. Build + test: `cd opendbc_repo && scons -j8` (or the project's build), then the safety + Ford unit tests (`pytest opendbc/safety/tests/test_ford.py`, the MADS suite). Confirm green.
9. Commit the opendbc merge; note the new opendbc HEAD sha.

### Phase 2 — main repo
10. In the superproject on `migrate-v2026.002.001`: `git merge upstream/master`.
11. Resolve the 7 files:
    - `modeld.py`: re-apply **LAT_SMOOTH_SECONDS=0.1** and verify the warp 4→2 output indexing still matches after the restructure (port manually, never bulk-take upstream).
    - `monitoring/helpers.py`: re-apply `_PHONE_THRESH=0.8`.
    - `common/model.h`, `sunnypilot/modeld_v2/SConscript`, `sunnypilot/models/tests/model_hash`: **study the upstream modeld directory restructure** (the `31dc4d8e52` tip is literally "fix cereal validation for upstream directory restructure"). Reconcile our `modeld_v2` path against the new layout; regenerate `model_hash` if it's a derived fixture.
    - `sunnypilot/sunnylink/params_metadata.json`: take upstream.
    - `opendbc_repo` submodule pointer: set to the Phase-1 merged opendbc HEAD (`git add opendbc_repo`).
12. **`common/params_keys.h` watch:** it did NOT conflict this round, but confirm our 4 custom keys (FordPath4Enabled, LaneBiasIntegral, disable_BP_long_UI, disable_downhill_comp_UI) are still present post-merge (`grep` them). If upstream added keys, hand-add — never wholesale-copy.
13. Build the main repo; run the relevant unit tests + a `replay`/process-replay on a saved route if available.
14. Commit the main-repo merge.

### Phase 3 — validate before deploy (gold standard)
15. **Offline:** confirm the lateral pipeline still produces identical commands — run `explorer_st_logs/pipeline_simulator.py` on a cached route pre/post-merge; the carcontroller output should match to <1e-5 1/m (our customizations unchanged). Run the `model_replay_sim` anchor to confirm modeld still loads/infers.
16. **On-device (only after offline green, and a deliberate decision):** `DisableUpdates=1` is already set; push the integration branch to `origin`, then on the device do a controlled checkout (NOT a wipe). Verify config live (smooth_tau, PI, model bundle), then a short shakedown drive before trusting.
17. If all green: fast-forward/merge `migrate-v2026.002.001` into `2021_explorer_st-mici`.

### Rollback
- Integration branch is disposable; `git switch 2021_explorer_st-mici` abandons it. Device stays on the current build until you explicitly check out the new branch. Keep the pre-merge opendbc + main HEAD shas noted in Phase 1/2.

## Honest caveats
- A clean textual merge ≠ semantic correctness. The 3 ⚠️ items (modeld restructure, lateral.h #456, LAT_SMOOTH/warp indexing) are where a silent break would hide — verify each with a build + the pipeline-simulator parity check, not just "it merged."
- The 248/150 "behind" is large in count but low in *relevance* — the June survey found the backend churn is mostly sunnylink/UI/release housekeeping with no Ford relevance. You're re-baselining for hygiene, not to gain features (except whatever's new in v2026.002.001 worth a separate survey).
- This plan was derived from a simulated merge in isolated worktrees; re-run the simulation if either upstream advances before you execute.

---

# EXECUTION LOG & FORWARD PLAN (updated 2026-06-30)

## What's been executed (on disposable branches — real branches + device UNTOUCHED)
- **opendbc `migrate-v2026.002.001` @ `5fde68e57`** — merged `sunnypilot/master` (150 commits). Conflicts: `carcontroller.py` = `--ours` (byte-identical parity, our 772 lines preserved); `libsafety_py.py` = took upstream's `ignition_can` decls. Verified: merged safety C compiles clean under the harness's strict flags (`-Wall -Wextra -Werror -std=gnu11`); `ford.h`/`mads.h`/`lateral.h` byte-identical to pre-merge; the 4 driving-code checks pass (steer-limit signature stable, AngleSteeringLimits compatible, no removed-`brake` use). **Safety pytest NOT run** (no cffi/msgq env here → deferred to dev env).
- **main `migrate-v2026.002.001` @ `4981fd151c`** — merged `upstream/master` (248 commits) + a checker-hardening commit. 7 conflicts resolved: `model.h` deleted (mechanism → `sunnypilot/models/model_name.py`), `monitoring/helpers.py` deleted + `_PHONE_THRESH=0.8` re-applied in `policy.py:46`, `params_metadata.json` deleted, `SConscript`/`model_hash` = theirs (build moved to `compile_modeld.py`), `selfdrive/modeld/modeld.py` = kept `LAT_SMOOTH=0.1` (dropped obsolete PKL consts), `opendbc_repo` submodule → `5fde68e57`. Verified: all customizations survived (LAT_SMOOTH, PHONE_THRESH, 4/4 param keys, longitudinal/torqued/card edits 100% present), no conflict markers, `py_compile` clean.
- **Params-key checker hardened** (`tools/check_params_keys.sh`) to exclude `test_*`/`/tests/`/`conftest` — fixes the upstream `test_params.py` `"swag"` false positive; hook now passes cleanly. Committed.

## Decisions (locked)
- **Adopt the new inference path** (the modeld restructure). Posture: openpilot/sunnypilot are beta; expect regular updates — staying current is the goal, with the hope the new engine is improved.
- **`DEFAULT_MODEL = "CD210"`** — keep upstream's value (true to sunnypilot); device's *active* model is a separate ModelManager param.
- Start with a **locally-compiled Nevada** bundle on the new engine, then CD210. **Re-anchor the replay sim** against the new engine. Freeze lateral tuning during the swap.

## Recommendations 1–7 (agreed)
1. **Model delivery — compile locally, don't trust auto-download.** `compile_modeld.py` (`--vision-onnx/--policy-onnx … --output combined.pkl`) over the ONNX already materialized at `retrospective_lateral/results/model_replay/onnx/{Nevada,CD210}/`. Verify the pkl loads in the new `modeld_v2` in the dev env before any device step. Nevada first (newer/cleaner under split logic), then CD210. Removes the "device downloads an incompatible bundle and won't boot" risk.
2. **Lateral baseline — single variable, measure before re-tuning.** Deploy new engine with `smooth_tau`/PI/`pc_blend` untouched; measure weave (validated scenes, 0.10–0.35 Hz band-RMS) offline then on-road; re-tune only if data shows regression.
3. **Parity splits in two.** (a) controller parity: identical logged `desiredCurvature` through pre/post `carcontroller` (`pipeline_simulator.py`) MUST match <1e-5 (any diff = real merge bug). (b) model behavioral A/B: re-anchored sim, old vs new engine on same frames — differences EXPECTED, judge quality not identity.
4. **Re-anchor — wrap the real engine, don't re-mirror.** Fresh read-only frame pull (≥`route_c5`); make the sim load+run the actual compiled new-format pkl (real `_run_policy`/`warp_enqueue`) instead of reimplementing the new warp/buffer (reimplementation caused the original 2× bug). Confirm new-engine replay reproduces logged `desiredCurvature` corr ≥0.95 on a native scene.
5. **DM model — provision alongside, shallow check.** Compile/deliver the DM model (`compile_dm_warp.py`) too; `PHONE_THRESH=0.8` re-applied; we don't read removed cereal DM fields → just confirm `dmonitoringmodeld` starts + DM alerts fire.
6. **Rollback + finalize.** Pre-deploy: tag pre-migration commit on both repos, back up current `/data/media` model bundle, keep `DisableUpdates=1`. Rollback = checkout old branch + restore bundle + reboot. Finalize order: push **opendbc** branch to fork FIRST (so `5fde68e57` exists remotely), then submodule pointer, then merge→push main. `ford.h` byte-identical → no forced Ford panda reflash.
7. **Gauntlet (dev env, fail-fast).** `scons` build both → opendbc safety pytest (`test_ford`+MADS) → new modeld tests (`test_compile_modeld`, `test_combined_pkl_loader`, `test_warp`) + monitoring tests → controller parity (3a) → model A/B (3b). No device step until all green.

## Integrated workflow (the chain)
1. Dev env: build + gauntlet pytest. 2. Compile Nevada pkl locally, verify it loads. 3. Frame pull + re-anchor sim to the real new engine. 4. Offline A/B old vs new engine weave → **decision gate: is the new engine acceptable?** 5. Snapshot + deploy Nevada (DisableUpdates), controller-parity confirmed, tuning frozen. 6. On-road shakedown + weave re-measure. 7. Finalize/push both repos; then repeat for CD210 + DM model.
Highest-leverage step = #1 (compile the bundle yourself) — converts the scariest unknown into a deterministic, dev-env-verifiable artifact.

---

## Dev-environment scope (prerequisite for Phases 1–2; the `.venv311` analysis env can't do build/pytest/compile)

**Confirmed (2026-06-30):** the `.venv311` analysis env lacks `cffi`/`msgq` and can't cleanly run `compile_modeld.py` (tinygrad is vendored + the repo's `openpilot` symlink resolves to the real pre-merge tree, not a worktree). So a proper openpilot dev env is the prerequisite. The repo already supports one natively on macOS.

**Path A — native macOS uv env (RECOMMENDED; repo is wired for it):**
- Toolchain present: `brew` (/opt/homebrew, Apple Silicon), `clang`, vendored `tinygrad_repo/`, SCons. Missing: **`uv`**.
- `tools/op.sh` detects Darwin; `tools/setup_dependencies.sh` handles macOS + runs `uv sync --frozen --all-extras`; Python pinned `3.12.13` (separate from the 3.11 analysis venv — keep both).
- Steps: (1) `brew install uv` (or the official installer); (2) `tools/op.sh setup` → brew deps + `uv sync` (creates the 3.12 venv with cffi/msgq/cereal/numpy/…); (3) `tools/op.sh build` → scons (msgq, cereal, libsafety, …); (4) `tools/op.sh test` → pytest.
- Scope note: macOS supports the **dev/test subset** we need (compile_modeld, opendbc safety tests, modeld/monitoring tests, analysis) — NOT the full on-device runtime (camerad/UI), which we don't need (that's the device).
- **Do the env on the migration branch in a clean checkout** (real repo or a properly-initialized worktree with submodules), NOT the scratchpad worktree (cross-symlink issues).

**Path B — Linux devcontainer (FALLBACK):** `Dockerfile.openpilot` present → reproducible full env if native build fights us. Heavier (container + CPU-only inference fine for compile/tests).

**Caveat:** `op setup` is a heavy, machine-modifying op (installs brew pkgs + a 3.12 toolchain + ~GB of deps, then a multi-minute scons build) — confirm before running. Once it exists, Phases 1 (build+gauntlet) and 2 (compile Nevada) run cleanly there; Phases 3–4 (sim re-anchor, A/B) can run in either env.
